services:
  sage-wiki-couchdb-init:
    build:
      dockerfile: Dockerfile.offline
      network: none
      additional_contexts:
        npm-packages: ${LLMWIKI_NPM_PACKAGES_DIR:-/srv/npm}

  sage-wiki-ingester:
    build:
      dockerfile: Dockerfile.offline
      network: none
      additional_contexts:
        npm-packages: ${LLMWIKI_NPM_PACKAGES_DIR:-/srv/npm}
